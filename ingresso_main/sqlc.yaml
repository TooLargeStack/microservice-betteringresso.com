version: "2"
sql:
  - engine: "postgresql"
    queries: "./internal/repository/query"
    schema: "./migrations"
    gen:
      go:
        package: "repository"
        sql_package: "pgx/v5"
        out: "./internal/repository"
        emit_interface: true
        emit_empty_slices: true
        emit_json_tags: true